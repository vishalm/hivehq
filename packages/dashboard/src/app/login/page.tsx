import LoginClient from './login-client'

export const metadata = {
  title: 'Sign In — HIVE',
  description: 'Sign in to the HIVE AI Consumption Network',
}

export default function LoginPage() {
  return <LoginClient />
}

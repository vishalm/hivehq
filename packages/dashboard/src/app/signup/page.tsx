import SignupClient from './signup-client'

export const metadata = {
  title: 'Create Account — HIVE',
  description: 'Create your HIVE account to start tracking AI token consumption',
}

export default function SignupPage() {
  return <SignupClient />
}
